apiVersion: v1
kind: Service
metadata:
  name: ms-consumers-service
  namespace: default
spec:
  selector:
    app: ms-consumers
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP 